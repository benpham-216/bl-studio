# Node.js Runner

The first isolated runner builds and tests TypeScript assets from an exact Git commit. Its input contract defines the repository, commit SHA, source path, manifest version, approved commands, limits and artifact destination.

Implementation begins only after the runner isolation ADR and threat model are accepted.

