# Contracts

Store versioned OpenAPI, JSON Schema and asynchronous event contracts here.

The first contracts are:

- Workspace, project and repository connection APIs
- Asset manifest
- Revision, review, build and release APIs
- Job command, progress and result event envelopes
- Template manifest

Events include schema version, event ID, correlation ID, causation ID, workspace ID, occurred time and idempotency key where applicable.

