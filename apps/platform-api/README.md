# Platform API

ASP.NET Core modular backend for synchronous platform rules.

Start with workspace/project authorization, repository connections, asset catalog, immutable revisions, reviews and releases. Use PostgreSQL migrations, optimistic concurrency and a transactional outbox. Expose OpenAPI as the source for generated web clients.

